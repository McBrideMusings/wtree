project_name = "Wtree"
python_min   = "3.11"
generator_commit = "369752ed5815575e0f1b326a45424409684d788d"
archetypes = ["go-cli"]

[commands.build]
desc = "Compile binary to bin/wtree"
steps = ["build"]
group = 1
priority = 10

[commands.deploy]
desc = "Install binary to ~/go/bin"
group = 1
priority = 20

[commands.test]
desc = "Run all tests"
steps = ["test"]
group = 2
priority = 10

[commands.vet]
desc = "Run go vet"
steps = ["vet"]
group = 2
priority = 20

[commands.fmt]
desc = "Format source files"
steps = ["fmt"]
group = 2
priority = 30

[commands.clean]
desc = "Remove build artifacts"
steps = ["clean"]
group = 2
priority = 40

[actions.build]
kind = "shell"
run = "go build -o bin/wtree ."

[actions.test]
kind = "shell"
run = "go test ./..."

[actions.vet]
kind = "shell"
run = "go vet ./..."

[actions.fmt]
kind = "shell"
run = "gofmt -w ."

[actions.clean]
kind = "shell"
run = "rm -rf bin/"
